# Reverse-image review

Install the official `dessant/search-by-image` extension in the managed analyst browser. For every asset in `assets/`, launch the approved engines, then complete `results.template.json`. A missing or blocked engine is not a negative result.
